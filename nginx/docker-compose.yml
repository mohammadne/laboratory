version: '3.8'
services:
  reverse-proxy:
    image: nginx:1.23.0
    container_name: reverse-proxy
    restart: "always"
    ports:
      - "8080:80"
    volumes:
      - ./configs/reverse-proxy.conf:/etc/nginx/conf.d/default.conf
    networks:
      - custom-network

  backend:
    image: nginx:1.23.0
    container_name: backend
    restart: "always"
    ports:
      - "8081:80"
    volumes:
      - ./configs/backend.conf:/etc/nginx/conf.d/default.conf
    networks:
      - custom-network

networks:
  custom-network:
    driver: bridge